# 이 파일을 .env 로 복사한 뒤 실제 값을 채우세요.
# .env는 반드시 .gitignore에 포함되어야 하며 git에 절대 커밋하지 마세요.

DB_USERNAME=shop
DB_PASSWORD=change-me-strong-password

# openssl rand -base64 64  로 생성 권장
JWT_SECRET=change-me-generate-a-long-random-secret
